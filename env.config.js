// Environment configuration for backend app
export const ENV_CONFIG = {
  API_BASE_URL: process.env.API_BASE_URL || 'https://0686fe28fffc.ngrok-free.app',
  PORT: process.env.PORT || 3000,
  // Add other environment variables as needed
}; 